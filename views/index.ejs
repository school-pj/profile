<!DOCTYPE html>
<html>

  <head>
    <title>ProfileApp</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <%- include('header',{user_name: user_name, password: password}) %>
  </head>

  <body>
    <h1><%= title %></h1>
  </body>
  <% if(user_name) { %>
  <div class="wrapper">
    <form action="/" method="post" class="task-form">
      <p><%= user_name %></p>
      <script>
        var id = req.session.id;
        function jump() {
          window.location.href = '/profile/' + id;
        }
      </script>
      <a href="javascript:void(0);" onclick="jump();">View My Profile</a>
      <p>自己紹介文<br>
        <textarea name="content" cols="30" rows="5"></textarea></p>
      <button type="submit" class="submit">投稿</button>
    </form>
    <br>
    <textarea readonly name="content" cols="50" rows="10"><%= contentList[0].content %></textarea>
    <% }else{ %>
    <form action="/signup" method="get" class="task-form">
      <button type="submit" class="submit">Sign up</button>
    </form>
    <% } %>
  </div>

</html>