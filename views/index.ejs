<!DOCTYPE html>
<html>

  <head>
    <title>TodoList</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>

  <body>
    <h1><%= title %></h1>
  </body>
  <div class="wrapper">
    <form action="/" method="post" class="task-form">
      <p><%= username %></p>
      <form action="/users/user_id">View My Profile</form>
      <form action="/users/user_id/follows">フォロー数：<%= follow ></form>
    <from action="/users/user_id/followers">フォロワー数：<%= follower ></from>
    <p>自己紹介文<br>
      <textarea name="content" cols="30" rows="5"></textarea></p>
      <button type="submit" class="submit">登録</button>
  </form>
</div>
</html>